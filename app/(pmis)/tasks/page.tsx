import { PlaceholderPage } from "@/components/ui/PlaceholderPage";
export default function TasksPage() {
  return <PlaceholderPage title="Tasks" stage={9} description="Approach-aware view (Timeline / Board / Split by workstream) — coming in Stage 9." />;
}
